import React from 'react'

const NewPersonCard = (props) => {
  return(
    <div>
      <h4>NewPersonCard</h4>
      
      <button>Make a new person</button>
    </div>
  )
}

export default NewPersonCard